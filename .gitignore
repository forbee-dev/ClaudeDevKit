.DS_Store
*.docx
*.pdf
TASKS.md
.plan.md

# .claude/ is local — forgebee/ is the canonical plugin source
.claude/

# Build artifacts
forgebee/ECC-ANALYSIS.md
# Packaged plugin tarball — generate at release time, never commit (stale binary in VCS).
forgebee.plugin

# Session working artifacts — plans + audits stay local
# (Framework docs live in README.md, CLAUDE.md, CHANGELOG.md, forgebee/README.md)
docs/planning/
