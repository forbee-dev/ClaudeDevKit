# Normalize line endings to LF across the tree so editors on different OSes
# don't churn diffs. Markdown personas and JS hooks had drifted to CRLF.
* text=auto eol=lf

# Binary artifacts — never normalize or diff as text.
*.pdf binary
*.docx binary
*.plugin binary
